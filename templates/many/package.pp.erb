class <%= @packages.first.module_name %>::<%= @packages.first.class_name %> {
<% @packages.each do |p| %>
  package {'<%= p.name %>':
    ensure   => <%= p.version %>,
    <% if p.provider %>
    provider => <%= p.provider %>,
    <% end %>
  }
<% end %>
}

class <%= @items.first.module_name %>::<%= @items.first.class_name %> {
<% @items.each do |item| %>
  package {'<%= item.name %>':
    ensure   => <%= item.version %>,
    <% if item.provider %>
    provider => <%= item.provider %>,
    <% end %>
  }
<% end %>
}

class <%= @item.module_name %>::<%= @item.name %> {
  package {'<%= @item.name %>':
    ensure   => <%= @item.version %>,
    <% if @item.provider %>
    provider => <%= @item.provider %>,
    <% end %>
  }
}
